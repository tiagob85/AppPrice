<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.teste.appprice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConfigActivity" />
        <activity android:name=".CadProdutosActivity" />
        <activity android:name=".CadMarcasActivity" />
        <activity android:name=".CadSecaoActivity" />
        <activity android:name=".CadLocalActivity" />
        <activity android:name=".PesqPrecosActivity" />
        <activity android:name=".VisualizarMarcaActivity" />
        <activity android:name=".VisualizarSecaoActivity" />
        <activity android:name=".VisualizarLocalActivity" />
        <activity android:name=".VisualizarProdutosActivity"></activity>
    </application>

</manifest>